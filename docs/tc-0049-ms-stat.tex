% TC-0049 fragment; intended for the compatibility test report.
\def\TCID{TC-0049}
\def\TCTitle{Microsoft 64-bit file status and x86 alias}
\def\TCPurpose{Verify the layout, status data, errors, symbols, and imports of
\texttt{\_stat64} and the x86 \texttt{\_stat} alias.}
\def\TCPriority{\TCPriorityCritical}
\def\TCRequirementRef{REQ-0049}
\def\TCDesignRef{Microsoft CRT documentation for the \texttt{\_stat} family
and verified TinyCC x86 linker behavior.}
\def\TCFeatureRef{\texttt{include/sys/stat.h}; presence and behavior fixtures;
\texttt{tests/mscompat/run-tc-0049.ps1}.}
\def\TCTestTechnique{ABI and symbol inspection, boundary-value, filesystem,
error, and import-table testing.}
\def\TCPreconditions{REQ-0003 is implemented and the harness can create
filesystem objects and inspect PE/COFF symbols.}
\def\TCEnvironment{TinyCC compilation for x86, x64, and ARM64; native
execution where supported; Windows 2000 import inspection on x86.}
\def\TCAssumptions{Sparse files may provide large-size evidence.}
\def\TCInputData{Regular, empty, large, read-only, and writable files;
directories; known timestamps; missing paths; and null arguments.}
\def\TCInitialState{Objects have recorded attributes, sizes, and times.}
\def\TCProcedure{Compile declarations and constants; verify member layout;
inspect symbols by architecture; query filesystem partitions; independently
compare fields; test failures; inspect imports and x86 alias resolution.}
\def\TCExpectedResult{\texttt{\_stat64} returns the required layout and data,
failures set the documented error, x86 resolves \texttt{\_stat}, and no
unintended alias or newer import appears elsewhere.}
\def\TCPostConditions{Test filesystem objects are removed.}
\TCTemplate
