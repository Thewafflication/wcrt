% TC-0050 fragment; intended for the compatibility test report.
\def\TCID{TC-0050}
\def\TCTitle{WPM bounded formatted-output integration}
\def\TCPurpose{Verify that WPM uses WCRT's complete \texttt{snprintf}
implementation without linking its reduced local formatter.}
\def\TCPriority{\TCPriorityCritical}
\def\TCRequirementRef{REQ-0050}
\def\TCDesignRef{REQ-0019, Microsoft CRT \texttt{snprintf} documentation,
and the approved TinyCC language-mode interface decision.}
\def\TCFeatureRef{\texttt{include/stdio.h};
\texttt{src/stdio\_format.c}; WPM integration fixture;
\texttt{tests/mscompat/run-tc-0050.ps1}.}
\def\TCTestTechnique{Build integration, declaration presence, symbol
inspection, boundary-value, and regression testing.}
\def\TCPreconditions{REQ-0019 is implemented and the selected WPM source
revision is available.}
\def\TCEnvironment{The supported TinyCC/WCRT WPM build configuration on each
WPM release architecture.}
\def\TCAssumptions{The interface decision records whether the standard
declaration or a temporary alias is selected.}
\def\TCInputData{WPM's string, character, signed, unsigned, hexadecimal,
width, and length-modifier formats; exact-fit and truncated destinations.}
\def\TCInitialState{WPM's reduced formatter is excluded and output buffers
contain sentinels.}
\def\TCProcedure{Compile WPM in its required language mode; inspect linked
symbols; exercise formatting and boundary paths; verify C99 returns and
termination; rerun TC-0019.}
\def\TCExpectedResult{WPM resolves the approved WCRT interface, no duplicate
formatter is linked, REQ-0019 behavior holds, and C89 isolation remains.}
\def\TCPostConditions{Only test-owned artifacts and buffers changed.}
\TCTemplate
