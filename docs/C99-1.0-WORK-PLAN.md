# C99 to 1.0.0 Work Plan

**Content type:** Project and personal work plan

**Status:** T6 local closure audited and R1 candidate prepared; release Reject

**Planning date:** 2026-08-04

**T2 baseline revision:** 2026-08-09

**T3 planning revision:** 2026-08-09

**T3 local implementation revision:** 2026-08-09

**T4 verification reconciliation:** 2026-08-12

**T5 planning revision:** 2026-08-12

**T6 planning revision:** 2026-08-15

**T6 implementation reconciliation:** 2026-08-17

**R1 local readiness revision:** 2026-08-17

**Owner:** WCRT maintainer

**Execution prompts:** `docs/C99-1.0-PROMPTS.md`

## Objective and Release Boundary

Release WCRT 1.0.0 after the required C99 hosted-library profile is specified,
implemented, reviewed, verified on x86, x64, and ARM64, and supported by a
completed release-readiness record. C89 behavior and the already baselined
Microsoft-compatibility interfaces remain regression obligations.

The remaining Microsoft CRT compatibility roadmap, C11 and later standards,
and optional `wWinMain` startup are not prerequisites for 1.0.0. `wWinMain`
may enter 1.0.0 only after the wide-character prerequisites pass and without
delaying C99 closure; otherwise it moves to 1.1.0.

## Planning Basis and Uncertainty

Repository history shows that the first controlled C99 tranche, REQ-0021
through REQ-0026, was baselined and implemented during 2026-07-26 through
2026-07-27. That tranche is useful structural evidence but is not a reliable
effort baseline because phase time and defects were not recorded. The estimates
below are therefore bottom-up ranges, not commitments.

Size is expressed as controlled conformance units (requirements with matching
test specifications), plus an indicative implementation-size range. Effort is
focused engineering time and excludes interruptions. Forecast dates shall be
set after two tranches have actual velocity; until then, the schedule is
expressed as sequential effort.

## Current Baseline

- REQ-0019, REQ-0021 through REQ-0033, and REQ-0035 through REQ-0042 have
  controlled requirements, specifications, implementation, manifest entries,
  and direct traceability. REQ-0034 remains an excluded optional extension.
- T6 audited all 24 C99 headers, the 15 inherited C89 header/runtime families,
  and clauses 7.1 through 7.25. The 75-row profile separately classifies
  normative class, implementation disposition, and per-target evidence.
- T6 corrected the C99 assertion function diagnostic, `isblank`, the complete
  C99 `lconv` layout, `MB_CUR_MAX` expression type, startup result aggregation,
  and the T4 fused-operation implementation. Decimal recommended practice is
  explicitly omitted rather than represented as universal correct rounding.
- The selected compiler package is TinyCC
  `0.9.28-rc.1442+2474e1c2`, source revision
  `d5c02f0fcdfdf75265d38df6ff9db2f8067367ac`. WPM 1.0.16, cv2pdb 0.54.0,
  the TinyCC release/key, and GitHub actions are pinned in the workflow.
- Local native x86/x64 C89, C99, compatibility, ABI, build, consumer, startup,
  and x86 import gates pass. Exact-revision run `32020695485` at `c75b259`
  confirms the full x86/x64 matrix and native ARM64 C89, build, consumers,
  startup, TC-0035/0036/0038, TC-0040--0042, and all other extension cases,
  but native ARM64 TC-0037 fails with access violation `0xC0000005`.
- Historical native ARM64 T4 evidence at
  `027b324e233d4a0c1912667835dbef31d61c6dcc` remains valid for that revision
  only. It is not transferred to the T6 candidate revision.
- C89 and REQ-0019 remain mandatory regressions throughout the program. The
  aggregate runner controls all C99 tests through TC-0042 plus Microsoft
  compatibility tests and rejects inventory differences from the manifest.
- The exact unsigned local candidate source is
  `3fa0b1a3a22bbd93ff9185dbb24689d83e057e56`; later CI corrections do not
  silently re-identify that package. The T6 decision is Reject because native
  ARM64 TC-0037 fails and independent project review remains incomplete. The
  R1 readiness decision is also Reject because a new exact candidate,
  signing, signed-byte Defender, WPM verification, install/rollback, and
  release-document gates are incomplete.

## Tranche Roadmap and Estimates

| Tranche | Scope | Size estimate | Effort estimate | Depends on | Exit evidence |
| --- | --- | --- | --- | --- | --- |
| T0 — Baseline correction | Reconcile REQ-0021–0026 status; add all C99 tests to the aggregate runner; add compiler capability probes; record data models | 1 process change, 6 requirement reconciliations, 6 test integrations, 6 facility probes, 3 data-model checks, 1 import gate; 450–850 changed lines | 21–36 h | Current CI | Green x86/x64/ARM64 C99 tranche and reviewed capability/import records |
| T1 — Integer core | REQ-0027 `inttypes.h`; REQ-0028 general utilities | 2 conformance units; 700–1,300 implementation LOC | 48–85 h | T0, `stdint.h`, wide declarations may be staged | Requirements, tests, implementation, range/error evidence |
| T2 — Narrow formatted I/O | REQ-0029, REQ-0030, narrow portion of REQ-0039 | 3 conformance units; 30–42 artifacts; 1,200–2,100 runtime/header LOC; 3,200–5,600 changed lines total | 130–225 h | T1; C-locale character ABI decision | All modifiers/conversions, `v*scanf`, hex-float, contract-separated regressions, native target results, and retained evidence |
| T3 — Wide ABI and character core | REQ-0033, then REQ-0031 and REQ-0032; impact closure for wide portions of REQ-0027, REQ-0039, and C99 time formatting | 3 primary conformance units, 9 impact reviews, about 79 functions; 40–56 artifacts; 7,800–13,300 changed lines total | 226–376 h base; 260–489 h completion forecast with 15–30% reserve | T0; T2 character/numeric cores; locale, encoding, stream, and `wint_t` ABI decisions | Accepted ADR/ABI record; complete wide/multibyte, formatting, classification, Windows-interoperation, and stream-orientation suites with retained native evidence on all targets |
| T4 — Real math and environment | REQ-0035 and REQ-0036 | 2 conformance units; actual 26 artifacts, 2,975 additions, and 254 removals | 118–208 h planned; focused actual unknown | Capability matrix, floating model | Native accuracy, classification, exception, and rounding-mode evidence; fused-operation disposition and independent review remain open |
| T5 — Complex and generic math | REQ-0037 and REQ-0038 | 2 primary conformance units; 1 compiler/ABI decision; 66 complex functions; 60 generic names; 26–40 artifacts; 2,400–4,800 production and 5,000–9,000 total changed lines | 174–300 h base; 209–405 h completion forecast with 20–35% reserve | T4 disposition; TinyCC 1441 language gate passes; ADR-0005 remains a regression fallback | Controlled requirements/specifications and implementation; x86/x64 native and ARM64 compile/link evidence pass; native ARM64 execution and independent review remain open |
| T6 — Integration closure | Remaining REQ-0039; REQ-0040 through REQ-0042; audit all 24 C99 headers and 15 inherited C89 runtime families | 4 conformance units; 38–62 artifacts; 4,800–9,600 changed lines including profile, requirements, tests, corrections, and evidence support | 174–322 h base; 218–451 h completion forecast with 25–40% discovery reserve | Clean reviewed T1–T5 baseline; T4 `fma` disposition; native T5 ARM64 and predecessor review/evidence closure | Complete clause/facility profile; no unclassified required row; C89 isolation, native x86/x64/ARM64 ABI/full regressions, build/package/consumer/import/evidence results |
| R1 — 1.0.0 release | Documentation closure, DFS/release controls, release candidate, signing, Defender, provenance, checksums, readiness decision, postmortem | 1 release baseline | 40–80 h | T6 and all common gates | Approved readiness record and immutable published artifacts |

The reconciled base total is **931–1,632 focused hours**. The earlier
506–868-hour total omitted T5 because its effort cell was missing. The T2 entry
review replaced its additive-parser assumption with the inspected cost of
correcting the shared numeric, varargs, bounded-output, and stream-scanning
paths and corrected a 5–10-hour arithmetic drift in the prior row total. The
T3 inspection then replaced an implementation-only estimate with the complete
59-function `<wchar.h>`, 18-function `<wctype.h>`, two deferred wide integer
conversions, C99 time-format dependency, stream-core change, controlled tests,
and evidence surface.

T3's inspected completion forecast applies a 15–30 percent reserve to its
226–376-hour base, yielding 260–489 hours. The base program total does not add
that reserve a second time. T2's size fell within its inspected range, but its
time log is empty; there is therefore no measured phase velocity from which to
reduce T3 hours or set a calendar date. T4 actual size is now known but focused
effort is not. T5 therefore receives its own inspected reserve rather than
inheriting an unsupported effort inference.

The T5 inspection replaces its 120–210-hour implementation-oriented placeholder
with a 174–300-hour controlled-work base. The added scope is the exact
66-function complex surface, 60-name type-generic map, compiler/ABI
qualification, numerical-vector provenance, C89 isolation, supported-target
consumers, and retained detailed evidence. The 20–35 percent T5 reserve is not
included in the base program total and excludes work in the TinyCC repository.
At 10, 20, or 30 focused hours per week the revised base is roughly 94–164,
47–82, or 32–55 weeks respectively, before contingency. No T5 calendar date is
forecast because focused effort actuals are unavailable and review gates remain.

The T6 inspection replaces its 80–140-hour, 800–1,600-line placeholder with a
174–322-hour controlled-work base. The inspected surface is four requirement
identifiers but 24 standard headers, 15 inherited C89 runtime families, a
clause/facility inventory, new requirements and specifications, correction
tests, three native architectures, Debug/Release build and package consumers,
legacy imports, traceability, source quality, and retained evidence. A 25–40
percent discovery reserve yields 218–451 hours and is not included in the base
program total. Unfinished predecessor corrections remain charged to their
originating tranche rather than being hidden in T6.

The T6 candidate, readiness draft, exact-CI corrections, retained native ARM64
finding, and failure diagnostics use 71 controlled artifacts and 5,250 changed
lines. Artifact count is nine above the 38--62 forecast; changed lines are
within the 4,800--9,600 range and 450 above its lower bound. Focused
effort remains unknown, so the hours forecast cannot be scored. The local
audit completed the planned classifications and in-scope corrections but
issued Reject rather than converting unavailable native or release evidence
to Pass.

T3's local staged snapshot contains 64 artifacts and 4,161 changed lines,
above the artifact forecast and below the changed-line forecast. The accepted
stateless C-locale design and reuse of T2 numeric/scanning cores explain most
of the line variance. The empty T3 time log still provides no basis for
revising the program's focused-hour total or setting a calendar date.

T4's inspected implementation spans 26 artifacts with 2,975 additions and 254
removals. Its focused-time log remains empty, so this size result cannot be
converted into a personal velocity or used to reduce T5 effort. T4's native
x86, x64, and ARM64 Debug CI gates passed at the exact implementation revision;
the fused-operation disposition and independent review remain open.

Tranches may proceed in parallel only when separate contributors or isolated
branches are available; the personal-process forecast assumes one maintainer
and sequential work.

## Required Work-Item Record

Before each tranche begins, create `docs/work/c99-tN-<name>.md` containing:

| Field | Required content |
| --- | --- |
| Scope and size | Requirement IDs, expected changed files or LOC range, exclusions |
| Estimate | Hours by plan/specify/design/implement/review/verify/postmortem and forecast range |
| Quality plan | Reviews, analyses, test levels, targets, and evidence |
| Time log | Date, phase, focused minutes, excluded interruption, note |
| Defect log | ID, type, injected phase, removed phase, fix minutes, related artifact |
| Checklist | Requirement, design, code, test, documentation, compatibility findings |
| Postmortem | Planned versus actual size/effort/schedule/defects and process changes |

Defect types are `requirements`, `design`, `interface/ABI`, `algorithm`,
`numeric`, `resource/lifetime`, `build/tooling`, `test`, and `documentation`.

## Completion Criteria for Every Tranche

- Planned requirements are reviewed and baselined before their implementation
  is represented as conforming.
- Each requirement maps to a controlled specification, runner, manifest entry,
  implementation, and retained result.
- Personal design/code/test review is complete and findings are dispositioned.
- Header self-containment, C89 isolation, supported-target ABI, and applicable
  Windows 2000 x86 imports are verified.
- C89, earlier C99, startup, source-quality, traceability, evidence, build, and
  consumer tests remain green.
- Implementation-defined behavior, deviations, risks, and compiler-blocked
  facilities are documented rather than silently omitted.
- Planned and actual measures and the postmortem are recorded.

## 1.0.0 Release Gates

1. REQ-0027 through REQ-0042 are either baselined and Pass, or explicitly
   classified by the C99 profile as optional, compiler-blocked, or an approved
   deviation consistent with the release claim.
2. No required C99, C89, compatibility, traceability, source-quality, build,
   package, or consumer result is Fail or unknown on a supported target.
3. The supported TinyCC versions, Windows data models, ABI, implementation-
   defined behavior, deviations, and platform validation tiers are published.
4. Phase 0 and C89 documentation gates relevant to the C99 release are closed.
5. Public API and ABI compatibility policy for the 1.x line is approved.
6. Release documentation, dependency provenance, exact artifact digests,
   signing/timestamp evidence, Defender results, rollback instructions, and
   support policy are complete.
7. A release-readiness record identifies the source revision and approves the
   exact 1.0.0 artifact set.
8. The release postmortem records estimate accuracy, defects, escaped issues,
   and selected process improvements.

## Principal Risks and Responses

| Risk | Response and decision point |
| --- | --- |
| TinyCC lacks consistent complex or type-generic behavior | The T6-selected TinyCC 1442 package passes complex and `_Generic` probes for all targets; retain ADR-0005's exact-diagnostic fallback, explicitly select mixed generic arguments to avoid its Windows `float + long double` conversion defect, and rerun all gates on package changes |
| Windows `long double` differs from abstract C99 expectations | Record each ABI and make tests follow the documented implementation model |
| Wide streams and UTF-16 expand scope | Baseline REQ-0033 design before T3 implementation; defer optional `wWinMain` first |
| Math accuracy work exceeds estimate | Define accuracy method and reference data in each test specification before implementation |
| Legacy Windows imports regress | Automate import inspection in T0 and gate every later tranche |
| Roadmap status drifts from evidence | Update roadmap checkboxes only in the same change that adds passing controlled evidence |
| Release controls remain deferred | Treat their closure as R1 deliverables, not post-release cleanup |
